{
  "$id": "searchFilesRequest.json",
  "type": "object",
  "properties": {
    "operation": { "type": "string", "const": "searchFiles" },
    "params": {
      "type": "object",
      "properties": {
        "query": { "type": "string" },
        "filters": { "type": "object" },
        "page": { "type": "integer", "minimum": 1, "default": 1 },
        "pageSize": { "type": "integer", "minimum": 1, "maximum": 100, "default": 20 }
      },
      "required": ["query"]
    }
  },
  "required": ["operation", "params"]
}
